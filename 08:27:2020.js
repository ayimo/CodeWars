function arr(n){
  let newArr = []; //create an array
  for(var i = 0; i < n; i++){
    newArr.push(i); //add i (the integer) to the array!! and then updates the length
  }
  return newArr;
}

//arr is secretly defined bc I can't use "Array" for a variable name ?
