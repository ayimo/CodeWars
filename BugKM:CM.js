function cockroachSpeed(s) {
  let result = Math.floor(s * 27.7778);
  return result
}

//math.floor "returns the largest integer
//less than or equal to a given number."

//And we wanted the answer to be an integer - and
//the input to be a decimal ("Real Number")
